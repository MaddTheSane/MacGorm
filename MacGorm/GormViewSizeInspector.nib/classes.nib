{
    IBClasses = (
	{
	    ACTIONS = {
		setAutosize = id;
	    };
	    CLASS = GormViewSizeInpector;
	    LANGUAGE = ObjC;
	    OUTLETS = {
		bottom = id;
		height = id;
		left = id;
		right = id;
		sizeForm = id;
		top = id;
		width = id;
	    };
	    SUPERCLASS = IBInspector;
	},
	{
	    CLASS = FirstResponder;
	    LANGUAGE = ObjC;
	    SUPERCLASS = NSObject;
	}
    );
    IBVersion = 1;
}